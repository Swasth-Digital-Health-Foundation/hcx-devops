apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hcx-provider-service.fullname" . }}-test-connection"
  labels:
    {{- include "hcx-provider-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hcx-provider-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
