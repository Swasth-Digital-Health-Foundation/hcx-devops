apiVersion: v1
kind: Service
metadata:
  name: {{ include "hcx-provider-service.fullname" . }}
  labels:
    {{- include "hcx-provider-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hcx-provider-service.selectorLabels" . | nindent 4 }}
